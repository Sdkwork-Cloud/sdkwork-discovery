# API Contracts

This directory contains API contracts and API source inputs for `sdkwork-discovery`.

## Structure

- `rpc/` - RPC/gRPC proto contracts and manifests

## Canonical Proto Source

The canonical proto source for `sdkwork-discovery` is located at:

- `proto/sdkwork/discovery/` - Discovery service proto definitions

See `../proto/` for the actual proto contract files.

## Related Specs

- `../sdkwork-specs/RPC_SPEC.md` - RPC/gRPC contract rules
- `../sdkwork-specs/RPC_SDK_WORKSPACE_SPEC.md` - RPC SDK workspace and generation rules
